# 电子病历库 · Agent 模板

用 **Obsidian + AI Agent** 归档体检报告、化验单、就医记录、用药史。本仓库是**框架与模板**（归档规则 + Agent 技能 + 空库骨架），**不含任何真实病历数据**。

## 如果你是 AI Agent，被要求用这个仓库干活 —— 按顺序读这几份

1. **`vault-template/AGENTS.md`** —— 归档规矩（**权威**，冲突时以它为准）
2. **`skills/health-vault/SKILL.md`**（连同 `references/`）—— 完整归档流程、字段规范、自检规则
3. **`vault-template/8-知识库/设计说明.md`** —— 为什么这么定；**改结构前必读**
4. 受控词表 → `vault-template/8-知识库/项目名规范.md`（报告项目名只能取自这里）
5. 想换方案 / 找现成轮子 → `vault-template/8-知识库/选型与参考项目.md`
6. 需要查药 → `skills/drug-lookup/SKILL.md`（**数据需自备，本仓库不附带**）

## 注意

- 真正的工作目录是**用户自己的 vault**（由 `vault-template/` 复制而来），不是本仓库。
- **硬边界**：只做整理与检索 —— ❌ 不诊断、❌ 不给用药建议、❌ 不判断紧急度、❌ 不补造数据（缺失标 `[信息缺失]`，推断标 `[推断]`，不确定标 `[待核对]`）。
- 几条最容易踩的规矩：**分不清就问**、**项目名走受控词表**、**多份单据先问归属**（7 天内的多张单子先确认是不是同一次看病）、**冲突必问**、**`1-基本信息/` 是基准线（默认只读）**。
- 详细说明见 `README.md`。
